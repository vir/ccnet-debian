#!/bin/sh
# postrm script for ccnet
#
# see: dh_installdeb(1)

#set -e

# summary of how this script can be called:
#        * <postrm> `remove'
#        * <postrm> `purge'
#        * <old-postrm> `upgrade' <new-version>
#        * <new-postrm> `failed-upgrade' <old-version>
#        * <new-postrm> `abort-install'
#        * <new-postrm> `abort-install' <old-version>
#        * <new-postrm> `abort-upgrade' <old-version>
#        * <disappearer's-postrm> `disappear' <overwriter>
#          <overwriter-version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package

U=ccnet

case "$1" in
    remove|upgrade|failed-upgrade|abort-install|abort-upgrade|disappear)
        ;;
    purge)
#        rm -rf /var/run/piportal
#	if [ -x `which ucf 2>/dev/null` ]
#	then
#		ucf --purge /usr/local/piportal/piportal.conf
#	fi
#	if [ -x `which ucfr 2>/dev/null` ]
#	then
#		ucfr --purge /usr/local/piportal/piportal.conf.dist /usr/local/piportal/piportal.conf
#	fi
#	rm -f /usr/local/piportal/piportal.conf /usr/local/piportal/piportal.conf.*
        deluser --system --quiet $U
        delgroup --system --quiet $U
        ;;
    *)
        echo "postrm called with unknown argument \`$1'" >&2
        exit 1
        ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0



